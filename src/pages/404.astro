---
/**
 * The only error page. GitHub Pages serves /404.html for every unknown
 * address, in either language, so this one page carries both.
 */
import NotFoundView from '../views/NotFoundView.astro';
---
<NotFoundView />
