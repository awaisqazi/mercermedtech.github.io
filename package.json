{
  "name": "mercermedtech-site",
  "type": "module",
  "version": "2.0.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "check:parity": "node scripts/check-parity.mjs",
    "check:links": "node scripts/check-links.mjs",
    "check:form": "node scripts/check-form-contract.mjs",
    "check:i18n": "node scripts/check-i18n.mjs",
    "check:seo": "node scripts/check-seo.mjs",
    "check:all": "npm run check:links && npm run check:form && npm run check:i18n && npm run check:seo && npm run check:parity",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/preact": "^6.0.5",
    "@astrojs/sitemap": "^3.7.4",
    "@supabase/supabase-js": "^2.116.0",
    "astro": "^7.3.3",
    "preact": "^10.29.8"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3"
  }
}
